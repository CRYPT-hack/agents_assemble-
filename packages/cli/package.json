{
  "name": "@assemble/cli",
  "version": "0.1.0",
  "description": "assemble — command line for the Agents Assemble workspace.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "assemble": "./dist/bin.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@assemble/core": "0.1.0",
    "@assemble/daemon": "0.1.0",
    "@assemble/mcp": "0.1.0"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
