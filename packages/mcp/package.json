{
  "name": "@assemble/mcp",
  "version": "0.1.0",
  "description": "MCP server that hands coding agents their Agents Assemble coordination tools.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "assemble-mcp": "./dist/bin.js"
  },
  "exports": {
    ".": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "dependencies": {
    "@assemble/core": "0.1.0",
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.5.2"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
